apiVersion: v1
kind: Service
metadata:
  name: {{ include "a-demo-service.fullname" . }}
  labels:
    {{- include "a-demo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a-demo-service.selectorLabels" . | nindent 4 }}
