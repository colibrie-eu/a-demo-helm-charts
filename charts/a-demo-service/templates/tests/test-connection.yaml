apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "a-demo-service.fullname" . }}-test-connection"
  labels:
    {{- include "a-demo-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "a-demo-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
